<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
        android:opacity="opaque">
    <item android:drawable="@color/joyconNeonRed" />

    <!-- TODO: use SVG instead of a bitmap.
            However, when I use SVG, the image doesn't get centered correctly... :(
    -->
    <item>
        <bitmap android:src="@mipmap/ic_splash" android:gravity="center" />
    </item>
</layer-list>
